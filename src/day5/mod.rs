use crate::*;

solver!("-n/a-", X, 0, 0);

struct X {}

impl X {
    fn parse(_input: &str) -> Self {
        Self {}
    }

    fn part_one(self) -> usize {
        0
    }

    fn part_two(self) -> usize {
        0
    }
}
